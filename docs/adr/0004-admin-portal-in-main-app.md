# Admin Portal in Main App

OmniDoxa hosts the admin portal inside the main Next.js application under protected `/admin` routes for the early build. This avoids a second deployment while the manual workflow is being proven.
